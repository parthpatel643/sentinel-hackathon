# M15 Chaos Drills — results

Generated by `scripts/chaos_drill.py` on 2026-09-23 11:03 UTC.

**2 passed, 0 failed, 2 skipped.**

Each drill breaks something real about a running stack and then watches the
platform's own API to see whether it noticed and recovered. A drill that could
not be run honestly on this machine is recorded as SKIPPED with the reason —
never as a pass.

| Drill | Outcome | Summary |
|---|---|---|
| `feed-loss` | **PASS** | dev-cam-01 left `live` 33s after its feed was killed, then recovered to `live` unattended once the feed returned, with the reconnect counted (0 -> 4). |
| `rtsp-blocked` | **PASS** | With RTSP unreachable, the same capture layer opened the HLS endpoint for dev-cam-01 and decoded 5 frames — the documented fallback works. |
| `scene-cut` | **SKIPPED** | No live external (e.g. government) camera to watch. The synthetic grid cannot produce a PTS discontinuity at all — see evidence/M2-SYNTHETIC-GRID-DISCONTINUITY-FINDING.md — so running this against it would report a pass it did not earn. Start a worker with `--source gov` and re-run. |
| `network-throttle` | **SKIPPED** | Requires root: macOS bandwidth shaping goes through pfctl/dnctl. Not simulated in-process, because throttling the application rather than the link would test something else entirely and the result would not mean what it claims. |

## feed-loss

- **Outcome:** PASS
- **Started:** 2026-09-23T11:02:21.138273+00:00
- **Duration:** 47.6s

```
[   0.1s] baseline: dev-cam-01 status=live reconnects=0 fps=25.0
[   0.1s] killed publisher for dev-cam-01 (pid 45521)
[  33.1s] platform noticed: status=down after 33.1s
[  33.2s] republished dev-cam-01 (pid 57916)
[  47.6s] recovered: status=live reconnects=4 fps=25.0 after 47.6s total
[  47.6s] dev-cam-01 left `live` 33s after its feed was killed, then recovered to `live` unattended once the feed returned, with the reconnect counted (0 -> 4).
```

## rtsp-blocked

- **Outcome:** PASS
- **Started:** 2026-09-23T11:03:08.718114+00:00
- **Duration:** 9.4s

```
[   0.1s] baseline: RTSP 127.0.0.1:8554 reachable=True
[   1.1s] with 8554 blocked (simulated via closed port 62581): RTSP open succeeded=False
[   9.4s] HLS fallback opened http://127.0.0.1:8888/dev/dev-cam-01/index.m3u8 and decoded 5 frame(s)
[   9.4s] With RTSP unreachable, the same capture layer opened the HLS endpoint for dev-cam-01 and decoded 5 frames — the documented fallback works.
```

## scene-cut

- **Outcome:** SKIPPED
- **Started:** 2026-09-23T11:03:18.126589+00:00
- **Duration:** 0.2s

```
[   0.2s] No live external (e.g. government) camera to watch. The synthetic grid cannot produce a PTS discontinuity at all — see evidence/M2-SYNTHETIC-GRID-DISCONTINUITY-FINDING.md — so running this against it would report a pass it did not earn. Start a worker with `--source gov` and re-run.
```

## network-throttle

- **Outcome:** SKIPPED
- **Started:** 2026-09-23T11:03:18.296800+00:00
- **Duration:** 0.0s

```
[   0.0s] Requires root: macOS bandwidth shaping goes through pfctl/dnctl. Not simulated in-process, because throttling the application rather than the link would test something else entirely and the result would not mean what it claims.
[   0.0s] To run manually: `sudo dnctl pipe 1 config bw 500Kbit/s` then pipe port 8554 traffic into it via an anchor in /etc/pf.conf, re-run the worker, and confirm cameras degrade rather than crash. `sudo dnctl -q flush` to restore.
```
